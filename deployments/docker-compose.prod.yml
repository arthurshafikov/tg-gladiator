services:
  app:
    build:
      context: ../.
      dockerfile: ./build/app/Dockerfile
      target: prod
    restart: always
    ports:
      - 80:80
      - 443:443
  adminer:
      image: adminer
      restart: on-failure
      ports:
          - "8210:8080"
